install scoop
install uv
install docker desktop (for local backend deployment)
install pulumi (for remote backend deployment),
(apt) install default-jre (for openapi generator)

# Relevant Ports and Locations

- swagger: `localhost:8000/docs`
- cloudbeaver: `localhost:8978`
- minio obj store: `localhost:9001` (creds listed under `S3`)
- creds: `root/services/.env`
