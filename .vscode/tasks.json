{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "🔄 uv sync all Python projects",
      "type": "shell",
      "command": "find . -maxdepth 3 -type f -name pyproject.toml -printf '%h\n' | uniq | xargs -I {} sh -c 'echo \"🔧 syncing {}\" && cd {} && uv sync --all-packages'",
      "problemMatcher": [],
      "runOptions": {
        // run this task automatically when the folder is opened
        "runOn": "folderOpen"
      },
      "presentation": {
        "reveal": "never",
        "panel": "shared",
        "revealProblems": "onProblem",
        "close": true
      }
    }
  ]
}
